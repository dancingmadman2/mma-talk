import 'package:flutter/material.dart';

class PredictionAlgo extends StatelessWidget {
  const PredictionAlgo({super.key});

  @override
  Widget build(BuildContext context) {
    return const Text('');
  }
}

double fighterRating(double strikeAccuracy) {
  return strikeAccuracy;
}
